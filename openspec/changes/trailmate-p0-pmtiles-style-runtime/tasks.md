## 1. PMTiles runtime style wiring

- [x] Add failing coverage for default runtime style construction.
- [x] Add failing coverage for complete local runtime style assets.
- [x] Add failing coverage for rejected network-backed runtime style assets.
- [x] Add a pure route style policy that resolves the style asset manifest.
- [x] Route `MapLibrePmTilesRouteMap` through the policy instead of directly through the factory.
- [x] Run targeted tests.
- [x] Run full Gradle tests and OpenSpec strict validation.
- [x] Request read-only review before PR.
